# Local AWS Environment

This environment is an isolated, low-cost AWS stack for integration testing.

It reuses the stage composition with a separate state backend, naming prefix, and domain.

Use this only for short-lived validation and pre-stage verification.
